# 开发者指南

本文档面向 ELA Chrome Extension 的开发者，包含开发环境设置、开发流程、代码规范、测试和发布等完整指南。

[English Version](./DEVELOPMENT.md) | [文档索引](./INDEX.md)

## 📋 目录

- [环境要求](#环境要求)
- [快速开始](#快速开始)
- [开发环境设置](#开发环境设置)
- [开发流程](#开发流程)
- [代码规范](#代码规范)
- [测试](#测试)
- [构建和打包](#构建和打包)
- [发布流程](#发布流程)
- [常见问题](#常见问题)

---

## 环境要求

- **Node.js**: >= 18.17.1（推荐使用最新 LTS 版本）
- **npm**: >= 9.6.7
- **Chrome**: 最新版本（用于测试扩展）
- **操作系统**: macOS / Linux / Windows

---

## 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/janewu77/ela-extension.git
cd ela-extension
```

### 2. 安装依赖

```bash
npm install
```

### 3. 启动开发模式

```bash
npm run watch
```

### 4. 加载扩展到 Chrome

1. 打开 Chrome，访问 `chrome://extensions/`
2. 开启"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择项目的 `dist/` 目录

---

## 开发环境设置

### 安装 Node.js 和 npm

如果尚未安装 Node.js，请访问 [Node.js 官网](https://nodejs.org/) 下载并安装。

验证安装：

```bash
node --version
npm --version
```

### 安装项目依赖

项目使用以下主要工具：

- **Parcel v2**: 构建工具
- **Tailwind CSS**: CSS 框架
- **Jest**: 测试框架
- **ESLint**: 代码质量检查
- **Prettier**: 代码格式化

所有依赖已配置在 `package.json` 中，运行 `npm install` 即可安装。

### 配置 Shell 脚本权限

```bash
cd sh
chmod +x copy-assets.sh
chmod +x zip.sh
chmod +x sync-version.sh
cd ..
```

### Mac OS X 语言设置（用于 i18n 测试）

如需测试多语言功能，需要配置系统语言：

1. 打开"系统偏好设置"
2. 选择"语言与地区"
3. 添加或调整语言顺序
4. 重启 Chrome

详细说明：[Chrome Extension i18n 文档](https://developer.chrome.com/docs/extensions/reference/api/i18n?hl=zh-cn)

### 编辑器配置（推荐）

#### VS Code

安装以下扩展：

- **ESLint** (`dbaeumer.vscode-eslint`)
- **Prettier - Code formatter** (`esbenp.prettier-vscode`)

在项目根目录创建 `.vscode/settings.json`：

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "[javascript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```

---

## 开发流程

### 日常开发

#### 1. 启动开发模式

```bash
npm run watch
```

此命令会：
- 同步版本号（`src/manifest.json` → `package.json`）
- 构建 CSS（开发模式）
- 复制静态资源
- 启动 Parcel 监听模式（自动重新构建）

#### 2. 修改代码

在 `src/` 目录下修改代码，Parcel 会自动重新构建。

#### 3. 测试扩展

在 Chrome 中重新加载扩展（`chrome://extensions/` → 点击刷新按钮）。

### 提交代码前

在提交代码前，请执行以下步骤：

```bash
# 1. 代码检查和格式化
npm run lint:fix && npm run format

# 2. 运行测试
npm test

# 3. 检查格式（可选）
npm run format:check
```

---

## 代码规范

项目使用 ESLint 和 Prettier 来保证代码质量和一致性。

### ESLint（代码质量检查）

**配置文件**: `eslint.config.mjs`

**主要规则**:
- 禁止使用 `var`，推荐使用 `const` 或 `let`
- 强制使用 `===` 而不是 `==`
- 警告未使用的变量（允许以 `_` 开头的变量）
- 允许使用 `console`（调试用）

**命令**:

```bash
# 检查代码问题
npm run lint

# 自动修复可修复的问题
npm run lint:fix
```

### Prettier（代码格式化）

**配置文件**: `.prettierrc`

**主要设置**:
- 使用双引号
- 2 个空格缩进
- 每行最大 100 个字符
- 使用分号
- ES5 兼容的尾随逗号

**命令**:

```bash
# 格式化所有代码
npm run format

# 检查格式（不修改文件）
npm run format:check
```

### 详细配置说明

更多配置细节请参考：[ESLint & Prettier 配置文档](./eslint-prettier-setup.md)

---

## 测试

项目使用 Jest 作为测试框架，测试文件位于 `src/__tests__/` 目录。

### 运行测试

```bash
# 运行所有测试
npm test

# 监听模式（自动运行变化的测试）
npm run test:watch

# 生成覆盖率报告
npm run test:coverage
```

### 测试覆盖率

覆盖率报告会生成在 `coverage/` 目录下，打开 `coverage/lcov-report/index.html` 查看详细报告。

### 编写测试

测试文件命名格式：`*.test.js`

示例：

```javascript
describe("功能模块名称", () => {
  beforeEach(() => {
    // 重置所有 mock
    jest.clearAllMocks();
  });

  it("应该描述测试场景", async () => {
    // 测试代码
    expect(result).toBe(expected);
  });
});
```

更多测试相关文档请参考：`src/__tests__/README.md`

---

## 构建和打包

### 开发构建

```bash
# 构建 CSS（开发模式）
npm run build:css

# 复制静态资源
npm run copy:assets
```

### 生产构建

```bash
# 完整打包流程（推荐）
npm run pack
```

此命令会：
1. 清理旧文件（`npm run clean`）
2. 同步版本号
3. 构建 CSS（生产模式）
4. 构建 JavaScript 和 HTML（Parcel）
5. 复制静态资源
6. 生成 zip 文件

### 分步执行

```bash
# 清理
npm run clean

# 构建
npm run build

# 打包
npm run zip
```

打包完成后，zip 文件会生成在 `archive/` 目录下，文件名格式：`ela_<version>.zip`

---

## 发布流程

### 发布前检查清单

在发布新版本前，请确保完成以下检查：

- [ ] **代码质量**
  - [ ] 代码已通过 ESLint 检查（`npm run lint`）
  - [ ] 代码已格式化（`npm run format`）
  - [ ] 所有测试通过（`npm test`）

- [ ] **版本管理**
  - [ ] 版本号已更新（`src/manifest.json`）
  - [ ] 版本号已同步到 `package.json`（运行 `npm run sync:version`）

- [ ] **构建和测试**
  - [ ] 构建成功（`npm run build`）
  - [ ] 打包成功（`npm run zip`）
  - [ ] 在 Chrome 中测试扩展功能正常
  - [ ] 测试所有主要功能（TTS、Chat、翻译等）

- [ ] **文档更新**
  - [ ] 更新 `doc/CHANGELOG.md` 和 `doc/CHANGELOG.zh.md` 记录变更
  - [ ] 如有重大变更，更新 `doc/RELEASE_NOTES_*.md`

### 快速发布命令

一键执行代码检查和格式化：

```bash
npm run check
```

一键执行完整的发布前检查（包括打包）：

```bash
npm run check && npm run pack
```

或者使用原来的多行命令：

```bash
npm run lint:fix && \
npm run format && \
npm test && \
npm run pack
```

### 发布步骤

1. **更新版本号**

   编辑 `src/manifest.json`，更新 `version` 字段：

   ```json
   {
     "version": "0.4.6"
   }
   ```

   运行版本同步脚本：

   ```bash
   npm run sync:version
   ```

2. **运行发布前检查**

   ```bash
   npm run lint:fix && npm run format && npm test
   ```

3. **构建和打包**

   ```bash
   npm run pack
   ```

4. **测试打包文件**

   - 解压 `archive/ela_<version>.zip`
   - 在 Chrome 中加载解压后的扩展
   - 测试所有功能

5. **提交代码**

   ```bash
   git add .
   git commit -m "chore: release v0.4.6"
   git tag v0.4.6
   git push origin dev
   git push origin v0.4.6
   ```

6. **更新文档**

   更新 `doc/CHANGELOG.md` 和 `doc/CHANGELOG.zh.md`：

   ```markdown
   #### 0.4.6
   
   - feature: 新功能描述
   - fix: 修复的问题
   - update: 更新的内容
   ```

---

## 常见问题

### Q: `npm run watch` 报错怎么办？

**A:** 检查以下几点：
1. 确保已安装所有依赖：`npm install`
2. 检查 Node.js 版本：`node --version`（需要 >= 18.17.1）
3. 检查 Shell 脚本权限：`chmod +x sh/*.sh`

### Q: 扩展加载后没有反应？

**A:** 
1. 检查 Chrome 控制台是否有错误（`chrome://extensions/` → 点击"检查视图"）
2. 确认 `dist/` 目录已正确构建
3. 检查 `manifest.json` 格式是否正确

### Q: ESLint 报错但代码能正常运行？

**A:** ESLint 检查的是代码质量和潜在问题，不影响运行。建议修复这些警告以提升代码质量。

### Q: 如何调试扩展？

**A:** 
1. **Background Script**: `chrome://extensions/` → 点击"service worker"链接
2. **Content Script**: 在网页中打开开发者工具（F12）
3. **Side Panel**: 右键点击扩展图标 → "检查弹出内容"

### Q: 测试失败怎么办？

**A:** 
1. 检查测试输出，查看具体错误信息
2. 确保 Chrome API mock 正确配置（`jest.setup.js`）
3. 运行单个测试文件：`npm test -- <test-file>`

### Q: 如何添加新的依赖？

**A:** 
1. 安装依赖：`npm install <package-name>`
2. 如果是开发依赖：`npm install <package-name> --save-dev`
3. 更新 `package.json` 后提交代码

### Q: 版本号不同步怎么办？

**A:** 
1. 确保 `src/manifest.json` 中的版本号是正确的
2. 运行：`npm run sync:version`
3. 检查 `package.json` 中的版本号是否已更新

---

## 项目结构

```
ela-extension/
├── src/                    # 源代码
│   ├── __tests__/         # 测试文件
│   ├── _locales/          # 国际化文件
│   ├── background.js      # Background Script
│   ├── manifest.json      # 扩展清单文件
│   ├── scripts/           # 脚本文件
│   │   ├── icons.js       # SVG 图标模块（统一管理图标）
│   │   ├── const.js       # 常量定义
│   │   ├── util.js         # 工具函数
│   │   └── ...
│   ├── sidepanels/        # 侧边栏文件
│   ├── options/           # 选项页面
│   └── css/               # 样式文件
├── dist/                  # 构建输出目录
├── archive/               # 打包文件目录
├── cfg/                   # 配置文件
├── sh/                    # Shell 脚本
├── doc/                   # 文档目录
├── eslint.config.mjs      # ESLint 配置
├── .prettierrc           # Prettier 配置
├── jest.config.js        # Jest 配置
└── package.json          # 项目配置
```

---

## 相关资源

- [Chrome Extension 官方文档](https://developer.chrome.com/docs/extensions/)
- [Parcel 文档](https://parceljs.org/)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [Jest 文档](https://jestjs.io/)
- [ESLint 文档](https://eslint.org/)
- [Prettier 文档](https://prettier.io/)

---

## 贡献指南

欢迎贡献代码！在提交 PR 前，请确保：

1. 代码已通过 ESLint 检查
2. 代码已格式化
3. 所有测试通过
4. 添加了必要的测试用例
5. 更新了相关文档

---

## 路线图 / 待办事项

计划中的功能和改进：

- 设定可导入/导出
- 支持其他 TTS、LLM 提供商
- 将内容导出至 Notion
- 下载声音文件

---

**最后更新**: 2026-01-20
